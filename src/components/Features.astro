---
const features = [
  {
    title: 'Ein Bildschirm. Alles drin.',
    description: 'Journal, Ziele und KI-Insights auf einer einzigen, übersichtlichen Oberfläche. Keine Tabs, keine Menüs, keine Ablenkungen.',
    icon: '📱',
    color: 'white'
  },
  {
    title: 'Tagesend-Analyse',
    description: 'Jeden Abend fasst die KI deine Gedanken zusammen und erstellt konkrete Handlungspläne für deine persönliche Entwicklung.',
    icon: '🧠',
    color: 'gray'
  },
  {
    title: 'Gezielte Vertiefung',
    description: 'Wähle einzelne Gedanken aus und führe strukturierte Gespräche mit der KI nach bewährten CBT-Prinzipien.',
    icon: '💭',
    color: 'white'
  }
];

const colorClasses = {
  white: {
    bg: 'bg-dark-800/50',
    border: 'border-light-900/20',
    hover: 'hover:border-light-700/30',
    icon: 'bg-light-900/10',
    title: 'text-light-900',
    glow: 'shadow-light-900/20'
  },
  gray: {
    bg: 'bg-dark-700/50',
    border: 'border-dark-600/40',
    hover: 'hover:border-light-600/30',
    icon: 'bg-dark-600/30',
    title: 'text-light-700',
    glow: 'shadow-dark-600/20'
  }
};
---

<section id="features" class="py-24 bg-dark-900 relative">
  <!-- Background -->
  <div class="absolute inset-0">
    <div class="absolute top-1/4 right-1/4 w-80 h-80 bg-light-900/2 rounded-full blur-3xl"></div>
    <div class="absolute bottom-1/4 left-1/4 w-64 h-64 bg-light-900/1 rounded-full blur-3xl"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-20 animate-slide-up">
      <div class="mb-4">
        <span class="inline-block px-4 py-2 bg-dark-700/50 text-light-700 border border-dark-600/50 rounded-full text-sm font-medium backdrop-blur-sm">
          ✨ Features
        </span>
      </div>
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-light-900 mb-6">
        <span class="text-light-900">
          Einfachheit
        </span>
        <br>
        <span class="text-light-600">die funktioniert</span>
      </h2>
      <p class="text-xl text-light-600 max-w-3xl mx-auto leading-relaxed">
        Keine komplizierten Menüs, keine endlosen Einstellungen. 
        Nur die Werkzeuge, die du wirklich brauchst.
      </p>
    </div>

    <!-- Features Grid -->
    <div class="grid lg:grid-cols-3 gap-8 lg:gap-12">
      {features.map((feature, index) => {
        const colors = colorClasses[feature.color];
        return (
          <div 
            class="group animate-slide-up"
            style={`animation-delay: ${index * 0.2}s;`}
          >
            <div class={`relative ${colors.bg} backdrop-blur-sm border ${colors.border} ${colors.hover} rounded-2xl p-8 h-full hover:scale-105 transition-all duration-500`}>
              <!-- Icon -->
              <div class="mb-6">
                <div class={`w-16 h-16 ${colors.icon} rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300`}>
                  <span class="text-3xl">{feature.icon}</span>
                </div>
              </div>

              <!-- Content -->
              <div>
                <h3 class={`text-2xl font-bold ${colors.title} mb-4 group-hover:text-white transition-colors duration-300`}>
                  {feature.title}
                </h3>
                <p class="text-light-600 leading-relaxed group-hover:text-light-500 transition-colors duration-300">
                  {feature.description}
                </p>
              </div>

              <!-- Hover Effect -->
              <div class="absolute inset-0 bg-gradient-to-br from-light-900/5 to-dark-600/5 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            </div>
          </div>
        );
      })}
    </div>

    <!-- Bottom CTA -->
    <div class="mt-20 text-center animate-slide-up" style="animation-delay: 0.8s;">
      <div class="bg-gradient-to-r from-dark-800/50 to-dark-700/50 backdrop-blur-sm border border-dark-600/50 rounded-2xl p-8 md:p-12 max-w-4xl mx-auto">
        <h3 class="text-2xl md:text-3xl font-bold text-light-900 mb-4">
          <span class="text-light-900">
            Bereit für mehr Klarheit?
          </span>
        </h3>
        <p class="text-lg text-light-600 mb-8 leading-relaxed max-w-2xl mx-auto">
          Lass dich nicht länger von unstrukturierten Gedanken überwältigen. 
          LoopCut hilft dir dabei, den Überblick zu behalten und konkrete Schritte zu planen.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <button class="group relative px-8 py-4 bg-light-900 text-dark-900 rounded-xl font-semibold hover:bg-light-800 transition-all duration-300 hover:scale-105">
            <span class="relative z-10">Jetzt herunterladen</span>
          </button>
          <button class="group relative px-8 py-4 bg-transparent border-2 border-dark-600/50 text-light-700 rounded-xl font-semibold hover:border-light-700/50 hover:text-light-600 transition-all duration-300">
            <span class="relative z-10">Mehr erfahren</span>
          </button>
        </div>
      </div>
    </div>
  </div>
</section> 