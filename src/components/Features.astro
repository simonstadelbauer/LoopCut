---
const features = [
  {
    icon: '🧠',
    title: 'Ein Bildschirm, alles drin',
    description: 'Journal, Ziele und KI-Insights vereint auf einem einzigen, übersichtlichen Bildschirm. Keine Tabs, keine Verwirrung.',
    color: 'orange'
  },
  {
    icon: '🤖',
    title: 'Smarte Tagesanalyse',
    description: 'Am Ende des Tages analysiert die KI deine Gedanken und erstellt automatisch strukturierte Aktionspläne für deine Zukunft.',
    color: 'blue'
  },
  {
    icon: '💭',
    title: 'Gezielte Vertiefung',
    description: 'Wähle spezifische Gedanken aus und führe tiefere KI-Gespräche nach bewährten CBT-Mustern für echte Durchbrüche.',
    color: 'gray'
  },
  {
    icon: '🎯',
    title: 'Fokus ohne Ablenkung',
    description: 'Keine Streaks, keine Gamification, keine Benachrichungen. Nur du, deine Gedanken und der Weg zu mehr Klarheit.',
    color: 'white'
  }
];

const colorClasses = {
  orange: {
    gradient: 'from-accent-orange/20 to-accent-orange/5',
    border: 'border-accent-orange/30',
    hover: 'group-hover:from-accent-orange/30 group-hover:to-accent-orange/10'
  },
  blue: {
    gradient: 'from-accent-blue/20 to-accent-blue/5',
    border: 'border-accent-blue/30',
    hover: 'group-hover:from-accent-blue/30 group-hover:to-accent-blue/10'
  },
  gray: {
    gradient: 'from-accent-gray/20 to-accent-gray/5',
    border: 'border-accent-gray/30',
    hover: 'group-hover:from-accent-gray/30 group-hover:to-accent-gray/10'
  },
  white: {
    gradient: 'from-light-900/10 to-light-900/5',
    border: 'border-light-900/20',
    hover: 'group-hover:from-light-900/20 group-hover:to-light-900/10'
  }
};
---

<section id="features" class="py-24 bg-dark-900 relative">
  <!-- Clean background -->
  <div class="absolute inset-0">
    <div class="absolute top-0 right-0 w-64 h-64 bg-accent-orange/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-48 h-48 bg-accent-blue/5 rounded-full blur-3xl"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-20 animate-slide-up">
      <div class="mb-4">
        <span class="inline-block px-4 py-2 bg-accent-orange/10 text-accent-orange border border-accent-orange/30 rounded-full text-sm font-medium backdrop-blur-sm">
          ✨ Features
        </span>
      </div>
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-light-900 mb-6">
        <span class="bg-gradient-to-r from-light-900 to-accent-orange bg-clip-text text-transparent">
          Einfachheit
        </span>
        <span class="text-light-800"> trifft Power</span>
      </h2>
      <p class="text-xl text-light-600 max-w-3xl mx-auto leading-relaxed">
        LoopCut kombiniert die Kraft der KI mit einem minimalistischen Design, 
        das dich dabei unterstützt, aus dem Gedankenkarussell auszubrechen.
      </p>
    </div>

    <!-- Features Grid -->
    <div class="grid md:grid-cols-2 gap-8 lg:gap-12">
      {features.map((feature, index) => {
        const colors = colorClasses[feature.color];
        return (
          <div 
            class={`group relative p-8 bg-gradient-to-br ${colors.gradient} backdrop-blur-sm border ${colors.border} rounded-2xl hover:scale-105 transition-all duration-500 animate-slide-up`}
            style={`animation-delay: ${index * 0.1}s;`}
          >
            <!-- Hover background effect -->
            <div class={`absolute inset-0 bg-gradient-to-br ${colors.gradient} ${colors.hover} rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500`}></div>
            
            <!-- Content -->
            <div class="relative z-10">
              <!-- Icon -->
              <div class="mb-6">
                <div class="w-16 h-16 bg-dark-800/50 rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                  <span class="text-3xl">{feature.icon}</span>
                </div>
              </div>
              
              <!-- Title -->
              <h3 class="text-2xl font-bold text-light-900 mb-4 group-hover:text-white transition-colors duration-300">
                {feature.title}
              </h3>
              
              <!-- Description -->
              <p class="text-light-600 leading-relaxed group-hover:text-light-500 transition-colors duration-300">
                {feature.description}
              </p>
            </div>
          </div>
        );
      })}
    </div>

    <!-- Bottom CTA -->
    <div class="text-center mt-20 animate-slide-up" style="animation-delay: 0.6s;">
      <div class="inline-flex items-center space-x-2 text-light-600 mb-4">
        <span class="w-12 h-px bg-gradient-to-r from-transparent via-accent-orange to-transparent"></span>
        <span class="text-sm font-medium">Bereit für den Wandel?</span>
        <span class="w-12 h-px bg-gradient-to-r from-transparent via-accent-orange to-transparent"></span>
      </div>
      <p class="text-lg text-light-700 max-w-2xl mx-auto">
        Entdecke, wie LoopCut deine täglichen Gedanken in kraftvolle Zukunftspläne verwandelt.
      </p>
    </div>
  </div>
</section> 