---
import AppStoreButton from "./AppStoreButton.astro";
const steps = [
  {
    number: '01',
    title: 'Gedanken festhalten',
    description: 'Schreibe deine täglichen Gedanken, Ideen und Ziele direkt in die App. Alles auf einem Bildschirm, ohne Ablenkungen.',
    icon: '✍️',
    color: 'white'
  },
  {
    number: '02',
    title: 'KI analysiert',
    description: 'Am Ende des Tages analysiert unsere KI deine Einträge und identifiziert Muster, Prioritäten und Handlungsfelder.',
    icon: '🧠',
    color: 'gray'
  },
  {
    number: '03',
    title: 'Strukturierte Pläne',
    description: 'Erhalte automatisch generierte Aktionspläne mit konkreten Schritten für deine persönliche und berufliche Entwicklung.',
    icon: '🎯',
    color: 'gray'
  },
  {
    number: '04',
    title: 'Vertiefte Gespräche',
    description: 'Wähle spezifische Gedanken aus und führe tiefere KI-Gespräche nach bewährten CBT-Methoden für echte Durchbrüche.',
    icon: '💭',
    color: 'white'
  }
];

const colorClasses = {
  white: {
    bg: 'bg-light-900/10',
    border: 'border-light-900/20',
    text: 'text-light-900',
    glow: 'shadow-light-900/20'
  },
  gray: {
    bg: 'bg-dark-600/30',
    border: 'border-dark-600/40',
    text: 'text-light-700',
    glow: 'shadow-dark-600/20'
  }
};
---

<section id="how-it-works" class="py-24 bg-dark-900 relative">
  <!-- Background Elements -->
  <div class="absolute inset-0">
    <div class="absolute top-1/4 left-1/4 w-64 h-64 bg-light-900/2 rounded-full blur-3xl"></div>
    <div class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-light-900/1 rounded-full blur-3xl"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-20 animate-slide-up">
      <div class="mb-4">
        <span class="inline-block px-4 py-2 bg-dark-700/50 text-light-700 border border-dark-600/50 rounded-full text-sm font-medium backdrop-blur-sm">
          🚀 Prozess
        </span>
      </div>
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-light-900 mb-6">
        <span class="text-light-900">
          So funktioniert's
        </span>
      </h2>
      <p class="text-xl text-light-600 max-w-3xl mx-auto leading-relaxed">
        In nur vier einfachen Schritten verwandelst du deine täglichen Gedanken 
        in strukturierte Zukunftspläne.
      </p>
    </div>

    <!-- Steps -->
    <div class="relative">
      <!-- Connection Line -->
      <div class="hidden lg:block absolute top-1/2 left-0 right-0 h-px bg-gradient-to-r from-transparent via-light-700/20 to-transparent transform -translate-y-1/2"></div>
      
      <div class="grid lg:grid-cols-4 gap-8 lg:gap-4">
        {steps.map((step, index) => {
          const colors = colorClasses[step.color];
          return (
            <div 
              class="relative animate-slide-up"
              style={`animation-delay: ${index * 0.2}s;`}
            >
              <!-- Step Card -->
              <div class="group relative bg-dark-800/50 backdrop-blur-sm border border-dark-700/50 rounded-2xl p-8 hover:scale-105 transition-all duration-500 hover:border-light-700/30">
                <!-- Step Number -->
                <div class="absolute -top-4 left-8">
                  <div class={`w-12 h-12 ${colors.bg} ${colors.border} border-2 rounded-2xl flex items-center justify-center backdrop-blur-sm group-hover:scale-110 transition-transform duration-300`}>
                    <span class={`text-lg font-bold ${colors.text}`}>
                      {step.number}
                    </span>
                  </div>
                </div>

                <!-- Content -->
                <div class="pt-8">
                  <!-- Icon -->
                  <div class="mb-6">
                    <div class="w-16 h-16 bg-dark-700/50 rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
                      <span class="text-3xl">{step.icon}</span>
                    </div>
                  </div>

                  <!-- Title -->
                  <h3 class="text-xl font-bold text-light-900 mb-4 group-hover:text-white transition-colors duration-300">
                    {step.title}
                  </h3>

                  <!-- Description -->
                  <p class="text-light-600 leading-relaxed group-hover:text-light-500 transition-colors duration-300">
                    {step.description}
                  </p>
                </div>

                <!-- Hover Effect -->
                <div class="absolute inset-0 bg-gradient-to-br from-light-900/5 to-dark-600/5 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
              </div>

              <!-- Connection Arrow (Desktop) -->
              {index < steps.length - 1 && (
                <div class="hidden lg:block absolute top-1/2 -right-2 transform -translate-y-1/2 z-20">
                  <div class="w-4 h-4 bg-light-700/20 rounded-full flex items-center justify-center">
                    <div class="w-2 h-2 bg-light-700 rounded-full animate-pulse"></div>
                  </div>
                </div>
              )}

              <!-- Connection Arrow (Mobile) -->
              {index < steps.length - 1 && (
                <div class="lg:hidden flex justify-center my-8">
                  <div class="w-px h-12 bg-gradient-to-b from-light-700/20 to-transparent"></div>
                </div>
              )}
            </div>
          );
        })}
      </div>
    </div>

    <!-- Bottom Section -->
    <div class="mt-24 text-center animate-slide-up" style="animation-delay: 1s;">
      <div class="bg-gradient-to-r from-dark-800/50 to-dark-700/50 backdrop-blur-sm border border-dark-600/50 rounded-2xl p-8 md:p-12 max-w-4xl mx-auto">
        <div class="mb-6">
          <span class="text-4xl mb-4 block">🎯</span>
          <h3 class="text-2xl md:text-3xl font-bold text-light-900 mb-4">
            <span class="text-light-900">
              Das Ergebnis?
            </span>
          </h3>
        </div>
        
        <p class="text-lg text-light-600 mb-8 leading-relaxed max-w-2xl mx-auto">
          Mehr Klarheit über deine Ziele, strukturierte Pläne für deine Zukunft 
          und die Werkzeuge, um aus dem endlosen Gedankenkarussell auszubrechen.
        </p>

        <div class="grid md:grid-cols-3 gap-6">
          <div class="text-center">
            <div class="text-3xl font-bold text-light-900 mb-2">✨</div>
            <div class="text-sm text-light-600">Mehr Klarheit</div>
          </div>
          <div class="text-center">
            <div class="text-3xl font-bold text-light-700 mb-2">🎯</div>
            <div class="text-sm text-light-600">Konkrete Pläne</div>
          </div>
          <div class="text-center">
            <div class="text-3xl font-bold text-light-900 mb-2">🚀</div>
            <div class="text-sm text-light-600">Echte Fortschritte</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section> 